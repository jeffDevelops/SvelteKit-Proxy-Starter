{
  // Tailwind v4 at-rules (@theme, @apply, @custom-variant, @plugin, …) are
  // processed by the Tailwind Vite plugin; the built-in CSS language server
  // doesn't know them and flags "Unknown at rule".
  "css.lint.unknownAtRules": "ignore"
}
